# 260304-v1 - Cron Page Layout Restructure

**Summary:**
Ubah layout halaman Cron dari header + dua kolom horizontal menjadi layout master-detail dengan jobs list di kiri (sidebar style) dan detail panel di kanan. Ketika tidak ada job yang di-edit, panel kanan menampilkan summary strip + runs list. Ketika sedang edit job, panel kanan menampilkan form.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Tambah fungsi helper `renderCronJobsPanel`, `renderCronSummaryStrip`, `renderCronRunsPanel`. Restructure `renderCron` dengan kondisi `isEditing` untuk toggle tampilan. |
| `ui/src/styles/components.css` | Layout baru `.cron-workspace` dengan 2 kolom, `.cron-jobs-panel` (sidebar style), `.cron-detail-panel` dengan independent scroll |

---

# 260304-v2 - Independent Scroll Panels

**Summary:**
Buat masing-masing panel (jobs list dan detail) memiliki scroll independen. Height disesuaikan dengan viewport agar tidak ada double scroll. Content-header hanya dirender ketika ada error untuk menghilangkan padding kosong.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/styles/layout.css` | Hapus vertical padding di `.content`, conditional render `.content-header` |
| `ui/src/ui/app-render.ts` | Conditional render content-header hanya saat `state.lastError` ada |
| `ui/src/styles/components.css` | Set fixed height `.cron-workspace`, independent overflow pada panel |

---

# 260304-v3 - Jobs Panel Sidebar Style

**Summary:**
Ubah jobs panel menjadi sidebar style: full height, background berbeda (`--bg-elevated`), border right, tanpa card styling. Lebar panel di-set 2x lebar sidebar (440px).

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Hapus class `card` dari jobs panel, hapus header "Jobs" dan subtitle |
| `ui/src/styles/components.css` | Styling `.cron-jobs-panel` sebagai sidebar dengan `background: var(--bg-elevated)`, `border-right`, padding adjustments |

---

# 260304-v4 - Search Field with Icon

**Summary:**
Hapus label "Search jobs" dan tambahkan icon kaca pembesar di dalam search field. Field dibuat full width mengikuti lebar container.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/icons.ts` | Tambah icon `moreHorizontal` untuk menu dropdown |
| `ui/src/ui/views/cron.ts` | Import icons, replace search label dengan icon di dalam field wrapper |
| `ui/src/styles/components.css` | Styling `.cron-filter-search` dengan icon absolute positioned di dalam input field |

---

# 260304-v5 - Job Item More Menu

**Summary:**
Pindahkan action buttons (Edit, Clone, Disable/Enable, Run, Run if due, History, Remove) dari card footer ke dropdown menu yang di-trigger oleh icon "more" (3 dots horizontal) di pojok kanan atas setiap job item. Remove button diberi styling danger.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/icons.ts` | Tambah icon `moreHorizontal` (3 dots) |
| `ui/src/ui/views/cron.ts` | Restructure `renderJob` dengan header containing title + more menu dropdown |
| `ui/src/styles/components.css` | Styling `.cron-job-header`, `.cron-job-menu` dropdown dengan items |

---

# 260304-v6 - Job Item Layout Fix

**Summary:**
Fix job item yang overflow karena prompt terlalu panjang. Job item sekarang menggunakan `display: block` dengan text wrapping untuk prompt yang panjang.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/styles/components.css` | Override `.cron-job.list-item` dengan `display: block`, tambah `word-break: break-word` untuk prompt, `min-width: 0` untuk prevent overflow |

---

# 260304-v7 - Filter Section Grid Layout

**Summary:**
Ubah filter section menjadi 2-kolom grid layout. Search field full width di atas, diikuti 6 filter fields dalam 3 baris dengan rasio 1:1.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/styles/components.css` | Set `.cron-jobs-panel .filters` dengan `display: grid`, `grid-template-columns: repeat(2, 1fr)`, search field dengan `grid-column: 1 / -1` |
