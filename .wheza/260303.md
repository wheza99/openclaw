# 260303-v1 - Light Mode Agent Avatar Background Fix

**Summary:**
Ubah background profile avatar agent pada light mode dari abu-abu menjadi putih, agar konsisten dengan background bubble chat.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/styles/chat/grouped.css` | Tambah override `.chat-avatar.assistant` di light mode menggunakan `--card` (#ffffff) |

---

# 260303-v2 - Chat Timestamp with Seconds

**Summary:**
Ubah format timestamp di chat bubble dari `h:mm` menjadi `h:mm:ss` agar bisa melihat selisih waktu kirim dan balas dengan presisi detik.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/chat/grouped-render.ts` | Tambah `second: "2-digit"` di `toLocaleTimeString()` untuk `renderStreamingGroup()` dan `renderMessageGroup()` |

---

# 260303-v3 - Fix Timestamp Accuracy from Session Transcript

**Summary:**
Fix timestamp yang tidak akurat karena frontend membaca `message.timestamp` (diset saat message masuk) bukan wrapper `timestamp` (diset saat response selesai). Sekarang timestamp menunjukkan waktu aktual response selesai.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `src/gateway/session-utils.fs.ts` | Override `message.timestamp` dengan wrapper timestamp (ISO string → Unix ms) yang lebih akurat |
