# 260305-v1 - Move Form Actions to Header

**Summary:**
Pindahkan button Cancel dan Submit dari bawah form ke pojok kanan atas, sejajar dengan title "Edit Job" dan subtitle.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Restructure edit form header dengan wrapper `.cron-form-header`, pindahkan buttons ke `.cron-form-header__actions` di sebelah kanan title |
| `ui/src/styles/components.css` | Tambah styling `.cron-form-header` (flex row, space-between), `.cron-form-header__actions` (button container), dan responsive handling untuk mobile (column layout) |

---

# 260305-v2 - Collapsible Form Sections

**Summary:**
Ubah semua card section (Basics, Schedule, Execution, Delivery) menjadi collapsible seperti card Advanced. Setiap section sekarang bisa di-collapse dengan klik pada header. Arrow indicator di pojok kanan menunjukkan state.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Ubah `<section>` menjadi `<details>` dengan `<summary>` untuk semua form sections (Basics, Schedule, Execution, Delivery) |
| `ui/src/styles/components.css` | Tambah styling untuk `.cron-form-section__summary` dengan arrow indicator pseudo-element, update `.cron-advanced` untuk konsistensi |

---

# 260305-v3 - Enlarge Collapse Toggle Icon

**Summary:**
Perbesar dan perjelas icon toggle-collapse di pojok kanan section header agar lebih terlihat.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/styles/components.css` | Ubah font size dari 10px ke 28px, color dari `var(--muted)` ke `var(--text-strong)`, tambah hover effect ke `var(--accent)` |

---

# 260305-v4 - Fix Schedule Section Field Alignment

**Summary:**
Perbaiki alignment field Expression dan Timezone di Schedule section. Tambahkan placeholder help text untuk konsistensi tinggi.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Tambah placeholder help text di bawah field Expression, pindahkan jitter help text ke bawah field Expression |
| `ui/src/styles/components.css` | Tambah `min-height: 17px` pada `.cron-help`, tambah `align-items: start` pada `.cron-form-grid` |

---

# 260305-v5 - Move Prompt Field to Top of Execution

**Summary:**
Pindahkan field "What should run?" (Assistant task prompt) ke posisi paling atas di section Execution, di atas field Session.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Reorder fields di Execution section, prompt textarea dipindah ke atas |

---

# 260305-v6 - Fullscreen Prompt Editor

**Summary:**
Tambahkan button fullscreen di samping label "Assistant task prompt" untuk expand textarea ke full screen. Memudahkan fokus saat menyusun prompt yang panjang.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/icons.ts` | Tambah icon `maximize` dan `minimize` |
| `ui/src/ui/views/cron.ts` | Tambah fullscreen button dengan toggle logic |
| `ui/src/styles/components.css` | Styling `.cron-prompt-field--fullscreen` untuk fullscreen mode |

---

# 260305-v7 - Textarea 12px Font and Wrapping

**Summary:**
Ubah semua textarea menjadi font size 12px dan wrapping (tidak scroll horizontal).

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/styles/components.css` | Set `.field textarea` dengan `font-size: 12px`, `white-space: pre-wrap`, `word-wrap: break-word` |

---

# 260305-v8 - Default Collapsed Sections with Dynamic Open

**Summary:**
Semua form sections collapse by default. Section Execution otomatis terbuka ketika mode Edit agar user langsung bisa edit prompt.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Hapus attribute `open` dari semua sections, tambahkan `?open=${isEditing}` pada Execution section |

---

# 260305-v9 - Asterisk on Required Section Titles

**Summary:**
Tambahkan asterisk (\*) di title section yang memiliki field required (Basics, Schedule, Execution). Delivery dan Advanced tanpa asterisk karena semua field optional.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Tambah `<span class="cron-required-marker">` di title Basics, Schedule, Execution |

---

# 260305-v10 - Filter Popover for Jobs Panel

**Summary:**
Pindahkan filter fields (Enabled, Schedule, Last Run, Sort, Direction, Reset) ke popover yang di-trigger oleh button filter di samping search field. Menghemat ruang di jobs panel.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/icons.ts` | Tambah icon `filter` |
| `ui/src/ui/views/cron.ts` | Restructure jobs panel header dengan search + filter button, pindahkan filters ke popover |
| `ui/src/styles/components.css` | Styling `.cron-jobs-header`, `.cron-filter-popover`, `.cron-filter-popover__panel` |

---

# 260305-v11 - Job Item Status Icon

**Summary:**
Tambahkan icon cron (loader) di samping nama job. Warna icon hijau jika enabled, merah jika disabled.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Tambah icon di `.cron-job-title` dengan class berdasarkan status |
| `ui/src/styles/components.css` | Styling `.cron-job-icon`, `.cron-job-icon--enabled` (hijau), `.cron-job-icon--disabled` (merah) |

---

# 260305-v12 - Simplify Job Item Display

**Summary:**
Hapus chips (enabled, isolated, now), agent info, dan delivery info dari job item. Tampilkan hanya schedule dan prompt saja. Prompt dibatasi maksimal 3 baris dengan ellipsis.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Hapus chip-row, agent info, delivery info dari `renderJob`. Simplify `renderJobPayload` |
| `ui/src/styles/components.css` | Hapus `.cron-job-chips`, tambah `.cron-job-prompt-clamp` dengan `-webkit-line-clamp: 3` |

---

# 260305-v13 - Advanced Section Consistency

**Summary:**
Ubah Advanced section menggunakan struktur yang sama dengan section lain (class `cron-form-section`) dengan subtitle di dalam summary. Subtitle tetap terlihat saat collapsed.

**Files Adjusted:**
| File | Changes |
|------|---------|
| `ui/src/ui/views/cron.ts` | Ubah `<details class="cron-advanced">` ke `<details class="cron-form-section">` dengan struktur summary yang sama |
